[pytest]
; добавляет корень проекта в sys.path, чтобы тесты могли делать `import app...`
pythonpath = .
; async-тесты (httpx.AsyncClient) не нужно помечать @pytest.mark.asyncio вручную
asyncio_mode = auto
